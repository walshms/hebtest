### POST localhost:8080/images

payloads should be of the form:
```json
{
    "enableObjectDetection": true,
    "image": "https://imagga.com/static/images/tagging/wolf-725380_640.jpg"
}
```
where image can either be the URL, or base 64 encoding of an image

